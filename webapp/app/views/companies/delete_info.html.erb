<h1 class="title">Delete Info</h1>
<hr>
<div class="row">
  <%= form_tag(perform_info_deletion_companies_path) do %>
    <div class="large-4 columns">
      <%= text_field_tag :ticker %>
    </div>
    <div class="large-4 columns">
      <%= text_field_tag :year, nil, placeholder: "Year" %>
    </div>
    <div class="large-4 columns">
      <%= submit_tag 'Delete', class: 'button postfix alert' %>
    </div>

  <% end %>
</div>

Delete Peers database
<%= form_tag(delete_db_companies_path) do %>
  <div class="field">
    <%= submit_tag 'Delete Peers database', class: 'big-button red', data: { confirm: "Are you sure you want to delete companies database?" } %>
  </div>
<% end %>
