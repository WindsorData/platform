<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_explanation" class="form-error cf">
      <h2 class="left"><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul class="left">
      <% @company.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :contact_email %><br />
    <%= f.text_field :contact_email %>
  </div>
  <div class="actions">
    <%= f.submit class: "button" %>
  </div>
<% end %>
