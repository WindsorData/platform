Company Ticker: <%= @company_peer.name %> (<%= @company_peer.ticker %>)

<%= form_tag(export_incoming_peers_raw_data_path) do %>
  <%= hidden_field_tag 'json_query', @json_query %>
  <%= submit_tag 'Download Incoming File', class: 'big-button' %>
<% end %>

<div class="panel">
  <% @companies_peers.each { |peer| %>
    <p><%= peer['companyName'] %>(<%= peer['ticker'] %>)</p>
  <% } %>
<div>