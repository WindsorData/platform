<div class="container cf">
  <div class="left">
    <%= link_to "Windsor", root_path, class: 'brand' %>
  </div>
  <div class="right">
    <% if user_signed_in? %>
      <%= current_user.email %>
      <%= link_to('Logout', destroy_user_session_path, method: 'delete') %>
    <% else %>
      <%= link_to('Login', new_user_session_path)  %>
    <% end %>
  </div>
</div>