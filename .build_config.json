{ "projects" :  
 [
   {
    "name" : "backend",
    "type" : "free",
    "directory" : "core",
    "commands" : ""
   },
   {
    "name" : "frontend",
    "type" : "rbenv",
    "directory" : "webapp",
    "commands" : "RAILS_ENV=integration bundle install && bundle exec rake db:drop db:setup && bundle exec rspec && bundle exec cucumber", 
    "override" : true
   }
 ]
}

